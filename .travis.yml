language: php
php:
  - 5.6
  - 7.0

before_install:
  - echo "extension=<extension>.so" >> ~/.phpenv/versions/$(phpenv version-name)/etc/php.ini
before_script:
  - composer install

script:
  - vendor/bin/phpunit
